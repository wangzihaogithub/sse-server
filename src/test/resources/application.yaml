server:
  port: 81
spring:
  servlet:
    multipart:
      location: ${user.dir}/src/test/resources/html
  thymeleaf:
    cache: false
    encoding: UTF-8
    mode: HTML
    prefix: classpath:/html/
    suffix: .html
  sse-server:
    remote:
      enabled: true
      nacos:
        server-addr: '10.0.65.98'
        namespace: hao
  application:
    name: 'xx-123'